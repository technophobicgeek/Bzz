<div class="pageTitle">
  <h1>Partners</h1>
</div>

<div class="toolbar">
  <div class="leftItem regularButton">
    <a href="<%= Rho::RhoConfig.start_path %>">Home</a>
  </div>
  <div class="rightItem regularButton">
    <a class="button" href="<%= url_for :action => :new %>">New</a>
  </div>
</div>

<div class="content">
  <ul>
    <% @partners.each do |partner| %>
    
        <li>
          <a href="<%= url_for :action => :show, :id => partner.object %>">
            <span class="title"><%= partner.bzzid %></span><span class="disclosure_indicator"></span>
          </a>
        </li>
    
    <% end %>
  </ul>
</div>