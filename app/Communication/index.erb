<div class="pageTitle">
  <h1>Communications</h1>
</div>

<div class="toolbar">
  <div class="leftItem regularButton">
    <a href="<%= Rho::RhoConfig.start_path %>">Home</a>
  </div>
  <div class="rightItem regularButton">
    <a class="button" href="<%= url_for :action => :new %>">New</a>
  </div>
</div>

<div class="content">
  <ul>
    <% @communications.each do |communication| %>
    
        <li>
          <a href="<%= url_for :action => :show, :id => communication.object %>">
            <span class="title"><%= communication.id %></span><span class="disclosure_indicator"></span>
          </a>
        </li>
    
    <% end %>
  </ul>
</div>